<div class="outer" data-post="{{id}}">
    <div class="header">
        <h3 class="post-title">{{title}}</h3>
        <h5 class="post-date">Posted by <b>{{user.username}}</b> on {{format_date created_at}}</h5>
    </div>
    <div class="content">
        <p>{{post_content}}</p>
    </div>
</div>